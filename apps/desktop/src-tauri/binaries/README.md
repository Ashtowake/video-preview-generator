# Sidecar Placeholders

These placeholder binaries exist so the Tauri app can compile in development before real FFmpeg sidecars are bundled.

Replace them with platform-specific `ffmpeg` and `ffprobe` executables during release packaging.
